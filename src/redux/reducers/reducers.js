const INICIAL_STATE = {
  playerEmail: '',
  playerName: '',
};

function reducers(state = INICIAL_STATE, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default reducers;
